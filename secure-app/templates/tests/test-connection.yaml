apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "secure-app.fullname" . }}-test-connection"
  labels:
    {{- include "secure-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "secure-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
